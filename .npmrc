# Defense-in-depth for npm-compatible installs.
# pnpm 11 reads non-registry settings from pnpm-workspace.yaml;
# keep this file for npm/older-tool fallback and contributor visibility.
ignore-scripts=true
save-exact=true
